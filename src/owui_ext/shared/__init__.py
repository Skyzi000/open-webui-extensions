"""Shared modules consumed by Open WebUI plugin sources.

Anything under ``owui_ext.shared`` is intended to be inlined into the released
single-file ``.py`` artifacts. At runtime in Open WebUI itself this package
does not exist -- only the inlined copies do.
"""
